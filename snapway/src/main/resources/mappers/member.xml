<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.snapway.model.mapper.MemberMapper">

	<resultMap type="member" id="memberMap">
		<id column="id" property="id"/>
		<result column="email" property="email"/>
		<result column="password" property="password"/>
		<result column="username" property="username"/>
		<result column="role" property="role"/> <!-- ENUM 타입 매핑 -->
		<result column="created_at" property="createdAt"/>
		<result column="profile_img" property="profileImg"/>
		<result column="gender" property="gender"/> <!-- ENUM 타입 매핑 -->
		<result column="birthday" property="birthday"/>
		<result column="style" property="style"/> <!-- ENUM 타입 매핑 -->
	</resultMap>

	<!-- 1. 회원가입 (INSERT) -->
	<insert id="registMember" parameterType="member" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
		INSERT INTO member (
			email, 
			password, 
			username, 
			role, 
			gender, 
			birthday, 
			style,
            created_at
		) VALUES (
			#{email}, 
			#{password}, 
			#{username}, 
			#{role}, 
			#{gender}, 
			#{birthday}, 
			#{style},
            now()
		)
	</insert>

	<!-- 2. 로그인 (SELECT) -->
	<select id="loginMember" parameterType="string" resultMap="memberMap">
		SELECT 
			id, 
			email, 
			password, 
			username, 
			role, 
			created_at, 
			profile_img, 
			gender, 
			birthday, 
			style
		FROM member
		WHERE email = #{email}
	</select>
    
    <!-- 3. 아이디(이메일) 중복 체크 (SELECT) -->
    <select id="checkEmail" parameterType="string" resultType="int">
        SELECT count(*)
        FROM member
        WHERE email = #{email}
    </select>

</mapper>